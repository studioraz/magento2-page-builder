<?php
/**
 * Copyright © 2024 Studio Raz. All rights reserved.
 * See LICENSE for license details.
 */
declare(strict_types=1);

use Magento\Framework\Escaper;
use Magento\Framework\View\Element\Template;

/**
 * @var Template $block
 * @var Escaper $escaper
 */

$breakpoints = [
    'mobile' => '(max-width: 639px)',
    'tablet' => '(min-width: 640px) and (max-width: 1023px)',
    'desktop' => '(min-width: 1024px)',
];
?>

<style>
    <?php foreach ($breakpoints as $name => $query): ?>
    @media <?= $query ?> {
        [data-content-type="slider"][data-has-size-<?= $name ?>="true"] [data-content-type="slide"],
        [data-content-type="slider"][data-has-size-<?= $name ?>="true"] .pagebuilder-slide-wrapper {
            aspect-ratio: var(--image-width-<?= $name ?>) / var(--image-height-<?= $name ?>);
        }

        [data-content-type="slider"][data-has-size-<?= $name ?>="true"] .pagebuilder-slide-wrapper {
            width: 100%;
            min-height: auto !important;
        }

        [data-content-type="slider"][data-has-size-<?= $name ?>="true"] .pagebuilder-overlay {
            min-height: auto !important;
        }

        [data-content-type="slider"][data-has-size-<?= $name ?>="true"] [data-element=content] {
            min-height: auto !important;
        }

        [data-content-type="slider"][data-has-size-<?= $name ?>="true"] {
            min-height: auto !important;
        }

        [data-content-type="slider"][data-has-size-<?= $name ?>="true"] [data-content-type="slide"] > div {
            display: flex;
            justify-content: center;
        }
    }
    <?php endforeach; ?>
</style>
